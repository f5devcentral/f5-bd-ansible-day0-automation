---

- name: show templating results metadata
  ansible.builtin.debug:
    var: "{{ lookup('ansible.builtin.template', 'metadata.yaml') }}"



- name: show templating results userdata
  ansible.builtin.debug:
    var: "{{ lookup('ansible.builtin.template', 'userdata.yaml') | to_yaml }}"